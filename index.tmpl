<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>WebAuthn Demo</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
  </head>

  <body>
    <p>The user is {{ .UserName }}</p>
    <h2>Repos:</h2>

    <form action="/delete_repo" method="POST">
      {{ range .Repos }} 
      <div>
        <p style="margin:0;display:inline;float:left">{{ . }}</p> 
        <button style="margin:0;display:inline:float:right" type="submit" id="postback1" name="repoName" value="{{ . }}">Delete</button>
      </div>
      {{ end }}
    </form>
  </body>

</html>
